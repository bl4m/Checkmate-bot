[project]
name = "checkmate-bot"
version = "0.1.0"
description = "Discord bot for the Somnium event: team roles, voice channels and moderation."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.12.14",
    "aiosqlite>=0.21.0",
    "asyncpg>=0.30.0",
    "certifi>=2025.7.14",
    "discord.py>=2.5.2",
    "PyNaCl>=1.5.0",
    "python-dotenv>=1.1.1",
]
